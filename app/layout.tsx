import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Gantto - Professional Project Timeline Manager",
  description: "Create beautiful Gantt charts with advanced features including natural language task creation, smart templates, and AI-powered insights. Perfect for project managers and developers.",
  keywords: "gantt chart, project management, timeline, task management, chrome extension, project planning",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Navbar />
        <main className="min-h-screen pt-16">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
